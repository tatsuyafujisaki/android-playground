# How to transfer a file from macOS to Android

```shell
adb push filename.extension /sdcard/
```

That said, if you want to send a file from macOS to an emulator, use drag-and-drop instead of `adb`.
This is the easiest way to do it.
